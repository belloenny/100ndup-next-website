overwrite: true
schema: "https://hndup-api-cms.herokuapp.com/graphql"
generates:
  ./graphql/types.tsx:
    plugins:
      - add: '/* eslint-disable */'
      - typescript
      - typescript-operations
  
    
